<div class="container">
	<ul>
		<% @movies.each do |movie| %>
		<li>
			<%= link_to(movie.title, movie_path(movie)) %> (<%= movie.year %>, <%= movie.director %>)
		</li>
		<% end %>
	</ul>

	<h5><%= link_to('Home', '/') %></h5>
</div>