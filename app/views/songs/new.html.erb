<%= form_for [@song] do |f|%>
	<%= f.label :title %>
	<%= f.text_field :title %>
	<br>
	<%= f.label :album %>
	<%= f.text_field :album %>
	<br>
	<%= f.label :artist %>
	<%= f.text_field :artist %>
	<br>

	<%= f.submit 'Save' %>
	<%= link_to('Back', movies_path) %>
<% end %>